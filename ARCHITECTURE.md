# Code Share - Architecture Design Document

## 📋 Table of Contents

- [Overview](#overview)
- [High-Level Architecture](#high-level-architecture)
- [Technology Stack](#technology-stack)
- [Directory Structure](#directory-structure)
- [Layer Architecture](#layer-architecture)
- [Data Flow](#data-flow)
- [Component Architecture](#component-architecture)
- [Security Architecture](#security-architecture)
- [State Management](#state-management)
- [Database Design](#database-design)
- [API Design](#api-design)
- [Scalability Architecture](#scalability-architecture)
- [Best Practices](#best-practices)
- [Testing Strategy](#testing-strategy)
- [Observability & Monitoring](#observability--monitoring)

---

## Overview

**Code Share** is a secure, real-time code sharing platform built with Next.js 16. It enables developers to create, share, and collaborate on code snippets with granular access controls.

### Key Features

- 🔐 **Secure Authentication** - Google OAuth via NextAuth.js
- 📝 **Rich Code Editor** - Monaco Editor with syntax highlighting
- 🔄 **Auto-Save** - Debounced auto-save with beacon API fallback
- 🎛️ **Access Control** - Public/Private visibility & collaborative editing
- 📊 **Storage Quotas** - User file limits with admin overrides
- 🎨 **Modern UI** - Tailwind CSS v4 with custom design system

---

## High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              CLIENT (Browser)                                │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────────────────┐  │
│  │   React 19      │  │  Monaco Editor  │  │      NextAuth Client        │  │
│  │   Components    │  │  (Code Input)   │  │      (Session State)        │  │
│  └────────┬────────┘  └────────┬────────┘  └──────────────┬──────────────┘  │
│           │                    │                          │                  │
│           └────────────────────┴──────────────────────────┘                  │
│                                    │                                         │
└────────────────────────────────────┼─────────────────────────────────────────┘
                                     │ HTTP/HTTPS
                                     ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                           NEXT.JS 16 SERVER                                  │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │                         App Router (RSC)                             │    │
│  │  ┌──────────────┐  ┌──────────────┐  ┌────────────────────────────┐ │    │
│  │  │ Server       │  │ API Routes   │  │    Server Actions          │ │    │
│  │  │ Components   │  │ /api/*       │  │    (Data Mutations)        │ │    │
│  │  └──────┬───────┘  └──────┬───────┘  └─────────────┬──────────────┘ │    │
│  └─────────┼─────────────────┼────────────────────────┼────────────────┘    │
│            │                 │                        │                      │
│            └─────────────────┴────────────────────────┘                      │
│                              │                                               │
│  ┌───────────────────────────┴───────────────────────────────────────────┐  │
│  │                        Business Logic Layer                            │  │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐   │  │
│  │  │ Auth Logic  │  │ Permissions │  │ Validations │  │  Constants  │   │  │
│  │  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘   │  │
│  └───────────────────────────┬───────────────────────────────────────────┘  │
│                              │                                               │
│  ┌───────────────────────────┴───────────────────────────────────────────┐  │
│  │                          Data Access Layer                             │  │
│  │  ┌─────────────────────────────────────────────────────────────────┐  │  │
│  │  │                    Mongoose ODM (Models)                         │  │  │
│  │  │  ┌─────────────────────┐     ┌─────────────────────────────┐    │  │  │
│  │  │  │     User Model      │     │       CodeFile Model        │    │  │  │
│  │  │  └─────────────────────┘     └─────────────────────────────┘    │  │  │
│  │  └─────────────────────────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
                                     │
                                     ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                              MONGODB ATLAS                                   │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │                         Database: share                              │    │
│  │  ┌─────────────────────┐          ┌─────────────────────────────┐   │    │
│  │  │  users collection   │◄────────►│    codefiles collection     │   │    │
│  │  └─────────────────────┘          └─────────────────────────────┘   │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## Technology Stack

| Layer          | Technology              | Purpose                          |
| -------------- | ----------------------- | -------------------------------- |
| **Frontend**   | React 19, Next.js 16    | UI Components & Server Rendering |
| **Styling**    | Tailwind CSS v4         | Utility-first CSS framework      |
| **Editor**     | Monaco Editor           | Rich code editing experience     |
| **Auth**       | NextAuth.js v4          | OAuth authentication             |
| **Validation** | Zod v4                  | Runtime schema validation        |
| **Database**   | MongoDB + Mongoose v9   | Document-based data storage      |
| **Icons**      | Lucide React            | SVG icon library                 |
| **Fonts**      | Poppins, JetBrains Mono | Typography                       |
| **Linting**    | ESLint v9, Prettier     | Code quality                     |
| **Hooks**      | Husky, lint-staged      | Pre-commit quality gates         |

---

## Directory Structure

```
code-share/
├── app/                          # Next.js App Router
│   ├── layout.tsx                # Root layout (Providers, Navbar)
│   ├── page.tsx                  # Home page (/)
│   ├── globals.css               # Global styles & Tailwind config
│   ├── error.tsx                 # Error boundary
│   ├── global-error.tsx          # Global error boundary
│   │
│   ├── actions/                  # Server Actions (modular)
│   │   ├── file.actions.ts       # CRUD operations for files
│   │   └── index.ts              # Barrel export
│   │
│   ├── api/                      # API Route Handlers
│   │   ├── auth/[...nextauth]/   # NextAuth endpoints
│   │   └── save-on-unload/       # Beacon save endpoint
│   │
│   ├── dashboard/                # User dashboard route
│   │   ├── page.tsx              # Dashboard page (SSR)
│   │   └── loading.tsx           # Suspense loading state
│   │
│   └── code/[id]/                # Dynamic code editor route
│       ├── page.tsx              # Code editor page (SSR)
│       └── loading.tsx           # Suspense loading state
│
├── components/                   # React Components
│   ├── Navbar.tsx                # Global navigation (RSC)
│   ├── Providers.tsx             # Client providers wrapper
│   │
│   ├── dashboard/                # Dashboard-specific components
│   │   ├── CreateFileButton.tsx  # New file creation
│   │   ├── FileCard.tsx          # File preview card
│   │   └── StorageQuota.tsx      # Usage indicator
│   │
│   ├── editor/                   # Editor-specific components
│   │   ├── CodeEditor.tsx        # Monaco wrapper (Client)
│   │   ├── EditorLoader.tsx      # Editor skeleton
│   │   ├── FileControls.tsx      # Settings controls
│   │   ├── FileEditor.tsx        # Main editor container
│   │   ├── FileHeader.tsx        # Title & metadata
│   │   └── SaveStatusIndicator.tsx
│   │
│   ├── shared/                   # Shared components
│   │   ├── SignIn.tsx            # Sign-in button
│   │   └── SignOut.tsx           # Sign-out button
│   │
│   ├── ui/                       # Base UI components
│   │   ├── Avatar.tsx
│   │   ├── Button.tsx
│   │   ├── CopyButton.tsx
│   │   ├── DropdownMenu.tsx
│   │   ├── Logo.tsx
│   │   ├── Select.tsx
│   │   ├── ShareButton.tsx
│   │   └── Skeleton.tsx
│   │
│   └── icons/                    # Icon components
│       └── index.tsx
│
├── enums/                        # TypeScript Enums
│   ├── app-routes.enum.ts        # Application routes
│   ├── auth-provider.enum.ts     # OAuth providers
│   ├── avatar-size.enum.ts       # Avatar sizing
│   ├── button-size.enum.ts       # Button sizing
│   ├── button-variant.enum.ts    # Button variants
│   ├── dropdown-item-variant.enum.ts
│   ├── file-edit-mode.enum.ts    # Owner/Collaborative
│   ├── file-visibility.enum.ts   # Public/Private
│   ├── http-status.enum.ts       # HTTP status codes
│   ├── language.enum.ts          # Supported languages
│   ├── save-status.enum.ts       # Save states
│   ├── user-role.enum.ts         # Admin/User roles
│   └── index.ts                  # Barrel export
│
├── hooks/                        # Custom React Hooks
│   ├── useAutoSave.ts            # Auto-save with debounce
│   ├── useClipboard.ts           # Copy to clipboard
│   ├── useFileSettings.ts        # File settings state
│   ├── useShare.ts               # Share functionality
│   └── index.ts                  # Barrel export
│
├── interfaces/                   # TypeScript Interfaces
│   ├── avatar.types.ts
│   ├── button.types.ts
│   ├── copy-button.types.ts
│   ├── dropdown-menu.types.ts
│   ├── dropdown.types.ts
│   ├── editor.types.ts
│   ├── file-card.types.ts
│   ├── file-controls.types.ts
│   ├── file-header.types.ts
│   ├── file.types.ts             # Core file types
│   ├── icon.types.ts
│   ├── select.types.ts
│   ├── share-button.types.ts
│   ├── storage-quota.types.ts
│   └── index.ts                  # Barrel export
│
├── lib/                          # Core Library Utilities
│   ├── auth.ts                   # NextAuth configuration
│   ├── cn.ts                     # Class name utility (clsx + twMerge)
│   ├── constants.ts              # Application constants
│   ├── db.ts                     # MongoDB connection singleton
│   ├── permissions.ts            # Authorization logic
│   ├── index.ts                  # Barrel export
│   │
│   └── seo/                      # SEO configuration
│       ├── config.ts             # Base metadata
│       ├── home.ts               # Home page metadata
│       ├── dashboard.ts          # Dashboard metadata
│       ├── code.ts               # Code page metadata
│       └── index.ts
│
├── models/                       # Mongoose Models
│   ├── User.ts                   # User document schema
│   └── CodeFile.ts               # Code file document schema
│
├── utils/                        # Utility Functions
│   ├── date.ts                   # Date formatting
│   ├── validations.ts            # Zod schemas
│   └── index.ts                  # Barrel export
│
├── public/                       # Static Assets
│   └── assets/
│       ├── images/               # User avatars, etc.
│       └── meta/                 # Favicons, PWA icons
│
└── [Config Files]                # Configuration
    ├── next.config.ts
    ├── tsconfig.json
    ├── eslint.config.mjs
    ├── postcss.config.mjs
    └── package.json
```

---

## Layer Architecture

### 1. Presentation Layer

```
┌─────────────────────────────────────────────────────────────────────┐
│                       PRESENTATION LAYER                             │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌─────────────────┐    ┌─────────────────┐    ┌────────────────┐   │
│  │  Server         │    │  Client         │    │    UI          │   │
│  │  Components     │    │  Components     │    │    Components  │   │
│  │  (RSC)          │    │  ('use client') │    │    (Reusable)  │   │
│  ├─────────────────┤    ├─────────────────┤    ├────────────────┤   │
│  │ • Navbar        │    │ • FileEditor    │    │ • Button       │   │
│  │ • Dashboard     │    │ • CodeEditor    │    │ • Avatar       │   │
│  │ • CodePage      │    │ • FileControls  │    │ • Select       │   │
│  │ • Layout        │    │ • Providers     │    │ • Dropdown     │   │
│  └─────────────────┘    └─────────────────┘    └────────────────┘   │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

### 2. Business Logic Layer

```
┌─────────────────────────────────────────────────────────────────────┐
│                      BUSINESS LOGIC LAYER                            │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐   │
│  │  Server Actions  │  │  API Routes      │  │  Permissions     │   │
│  ├──────────────────┤  ├──────────────────┤  ├──────────────────┤   │
│  │ • createCodeFile │  │ • POST /api/     │  │ • canCreateFile  │   │
│  │ • updateCodeFile │  │   save-on-unload │  │ • canEditFile    │   │
│  │ • deleteCodeFile │  │                  │  │                  │   │
│  │ • updateSettings │  │ • NextAuth APIs  │  │                  │   │
│  └──────────────────┘  └──────────────────┘  └──────────────────┘   │
│                                                                      │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐   │
│  │  Custom Hooks    │  │  Validations     │  │  Constants       │   │
│  ├──────────────────┤  ├──────────────────┤  ├──────────────────┤   │
│  │ • useAutoSave    │  │ • codeFileSchema │  │ • MAX_CONTENT    │   │
│  │ • useFileSettings│  │ • objectIdSchema │  │ • MAX_FILES      │   │
│  │ • useClipboard   │  │ • contentUpdate  │  │ • DEBOUNCE_MS    │   │
│  │ • useShare       │  │                  │  │                  │   │
│  └──────────────────┘  └──────────────────┘  └──────────────────┘   │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

### 3. Data Access Layer

```
┌─────────────────────────────────────────────────────────────────────┐
│                       DATA ACCESS LAYER                              │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │                    Mongoose Models                            │   │
│  ├──────────────────────────────────────────────────────────────┤   │
│  │                                                               │   │
│  │  ┌─────────────────────┐      ┌─────────────────────────┐    │   │
│  │  │      User           │      │       CodeFile          │    │   │
│  │  ├─────────────────────┤      ├─────────────────────────┤    │   │
│  │  │ • name: String      │      │ • title: String         │    │   │
│  │  │ • email: String     │◄─────│ • language: String      │    │   │
│  │  │ • image: String     │      │ • content: String       │    │   │
│  │  │ • providerId: String│      │ • visibility: Enum      │    │   │
│  │  │ • role: Enum        │      │ • editMode: Enum        │    │   │
│  │  │ • timestamps        │      │ • createdBy: ObjectId   │    │   │
│  │  └─────────────────────┘      │ • timestamps            │    │   │
│  │                               └─────────────────────────┘    │   │
│  │                                                               │   │
│  └──────────────────────────────────────────────────────────────┘   │
│                                                                      │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │                    Database Connection                        │   │
│  ├──────────────────────────────────────────────────────────────┤   │
│  │  lib/db.ts - Singleton connection with caching               │   │
│  │  • Global connection cache (prevents multiple connections)   │   │
│  │  • Buffer commands disabled for serverless                   │   │
│  └──────────────────────────────────────────────────────────────┘   │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

---

## Data Flow

### Authentication Flow

```
┌──────────┐     ┌───────────┐     ┌──────────────┐     ┌─────────┐
│  User    │────►│ SignIn    │────►│   NextAuth   │────►│ Google  │
│  Click   │     │ Component │     │   /api/auth  │     │ OAuth   │
└──────────┘     └───────────┘     └──────────────┘     └────┬────┘
                                                              │
     ┌────────────────────────────────────────────────────────┘
     │
     ▼
┌─────────────┐     ┌──────────────┐     ┌──────────────┐
│  Callback   │────►│  signIn CB   │────►│  Create/Find │
│  from       │     │  (lib/auth)  │     │  User in DB  │
│  Google     │     └──────────────┘     └──────┬───────┘
└─────────────┘                                  │
                                                 ▼
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│  Redirect    │◄────│  JWT Token   │◄────│  session CB  │
│  to App      │     │  Created     │     │  Enriches    │
└──────────────┘     └──────────────┘     └──────────────┘
```

### File Creation Flow

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│  Create File │────►│  Server      │────►│  Validate    │
│  Button      │     │  Action      │     │  Session     │
└──────────────┘     └──────────────┘     └──────┬───────┘
                                                  │
                                                  ▼
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│  Check       │────►│  Validate    │────►│  canCreate   │
│  File Limit  │     │  Input (Zod) │     │  Permission  │
└──────────────┘     └──────────────┘     └──────┬───────┘
                                                  │
                                                  ▼
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│  Redirect    │◄────│  Revalidate  │◄────│  Create      │
│  to Editor   │     │  Dashboard   │     │  Document    │
└──────────────┘     └──────────────┘     └──────────────┘
```

### Auto-Save Flow

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│  User Types  │────►│  setContent  │────►│  Debounce    │
│  in Editor   │     │  Hook        │     │  (1500ms)    │
└──────────────┘     └──────────────┘     └──────┬───────┘
                                                  │
                                                  ▼
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│  Update      │◄────│  Validate    │◄────│  save()      │
│  Status UI   │     │  Content Len │     │  Function    │
└──────────────┘     └──────────────┘     └──────┬───────┘
                                                  │
                                                  ▼
                     ┌──────────────┐     ┌──────────────┐
                     │  Update      │◄────│  Server      │
                     │  lastSaved   │     │  Action      │
                     └──────────────┘     └──────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│  FALLBACK: On page unload (beforeunload event)                      │
│  ┌──────────────┐     ┌──────────────┐     ┌──────────────┐        │
│  │  Page        │────►│  Check       │────►│  sendBeacon  │        │
│  │  Unload      │     │  Unsaved     │     │  to API      │        │
│  └──────────────┘     └──────────────┘     └──────────────┘        │
└─────────────────────────────────────────────────────────────────────┘
```

---

## Component Architecture

### Component Hierarchy

```
RootLayout
├── Providers (SessionProvider)
│   ├── Navbar (RSC)
│   │   ├── Logo
│   │   ├── Avatar
│   │   ├── SignIn / SignOut
│   │   └── Dashboard Link
│   │
│   └── {children} ◄─── Page Content
│
├── Home Page (RSC)
│   ├── SignIn Button (Guest)
│   └── Create File Link (Authenticated)
│
├── Dashboard Page (RSC)
│   ├── CreateFileButton
│   ├── StorageQuota
│   └── FileCard[] (Grid)
│
└── Code/[id] Page (RSC)
    └── FileEditor (Client)
        ├── FileHeader
        │   └── Editable Title
        ├── FileControls
        │   ├── Language Select
        │   ├── Visibility Select
        │   ├── EditMode Select
        │   └── Delete Button
        ├── ShareButton
        ├── CopyButton
        ├── CodeEditor (Monaco)
        └── FileMetaInfo
            └── SaveStatusIndicator
```

### Component Communication Patterns

```
┌─────────────────────────────────────────────────────────────────────┐
│                    FileEditor (State Container)                      │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  State:                                                              │
│  • content, title (from useAutoSave)                                │
│  • language, visibility, editMode (local)                           │
│  • isSaving, saveStatus, saveError                                  │
│                                                                      │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │                         Props Down                           │    │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐    │    │
│  │  │FileHeader│  │FileCtrl  │  │CodeEditor│  │MetaInfo  │    │    │
│  │  │ title ▼  │  │ lang ▼   │  │ code ▼   │  │ status ▼ │    │    │
│  │  │ canEdit ▼│  │ vis ▼    │  │ readOnly▼│  │ isSaving▼│    │    │
│  │  └────┬─────┘  └────┬─────┘  └────┬─────┘  └──────────┘    │    │
│  │       │             │             │                         │    │
│  │  ┌────┴─────────────┴─────────────┴─────────────────────┐  │    │
│  │  │                  Events Up                            │  │    │
│  │  │  onTitleChange ▲  onSettingsUpdate ▲  onChange ▲      │  │    │
│  │  └───────────────────────────────────────────────────────┘  │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

---

## Security Architecture

### Defense in Depth Model

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           SECURITY DEFENSE LAYERS                            │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │  Layer 1: NETWORK & TRANSPORT SECURITY                                 │ │
│  ├────────────────────────────────────────────────────────────────────────┤ │
│  │  • HTTPS/TLS 1.3 encryption (Vercel/hosting provider)                  │ │
│  │  • HTTP Strict Transport Security (HSTS)                               │ │
│  │  • DNS-level protection (Cloudflare/provider)                          │ │
│  │  • DDoS mitigation at edge                                             │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                     ▼                                        │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │  Layer 2: APPLICATION SECURITY                                         │ │
│  ├────────────────────────────────────────────────────────────────────────┤ │
│  │  • Next.js security headers (CSP, X-Frame-Options, etc.)               │ │
│  │  • CSRF protection via Server Actions (automatic)                      │ │
│  │  • XSS prevention (React's automatic escaping)                         │ │
│  │  • Content Security Policy enforcement                                 │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                     ▼                                        │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │  Layer 3: AUTHENTICATION (NextAuth.js)                                 │ │
│  ├────────────────────────────────────────────────────────────────────────┤ │
│  │  • OAuth 2.0 / OpenID Connect (Google)                                 │ │
│  │  • JWT tokens with secure signing (NEXTAUTH_SECRET)                    │ │
│  │  • HttpOnly, Secure, SameSite cookies                                  │ │
│  │  • Session expiry & refresh handling                                   │ │
│  │  • No password storage (delegated to OAuth provider)                   │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                     ▼                                        │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │  Layer 4: AUTHORIZATION (lib/permissions.ts)                           │ │
│  ├────────────────────────────────────────────────────────────────────────┤ │
│  │  • Role-based access control (RBAC): USER, ADMIN                       │ │
│  │  • Resource-level permissions (owner, collaborative)                   │ │
│  │  • Principle of least privilege                                        │ │
│  │  • Permission checks on every mutation                                 │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                     ▼                                        │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │  Layer 5: INPUT VALIDATION & SANITIZATION                              │ │
│  ├────────────────────────────────────────────────────────────────────────┤ │
│  │  • Zod schema validation on all inputs                                 │ │
│  │  • MongoDB ObjectId format validation                                  │ │
│  │  • Content length limits (500KB max)                                   │ │
│  │  • Null byte removal & content sanitization                            │ │
│  │  • Title length restrictions (1-100 chars)                             │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                     ▼                                        │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │  Layer 6: DATA SECURITY                                                │ │
│  ├────────────────────────────────────────────────────────────────────────┤ │
│  │  • MongoDB Atlas encryption at rest (AES-256)                          │ │
│  │  • TLS encryption in transit                                           │ │
│  │  • Environment variables for secrets                                   │ │
│  │  • No sensitive data in client bundles                                 │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### OWASP Top 10 Mitigation

| OWASP Risk                      | Mitigation Strategy                                          | Status |
| ------------------------------- | ------------------------------------------------------------ | ------ |
| **A01: Broken Access Control**  | RBAC, resource ownership checks, server-side validation      | ✅     |
| **A02: Cryptographic Failures** | HTTPS, JWT signing, no password storage, env secrets         | ✅     |
| **A03: Injection**              | Mongoose ODM (parameterized), Zod validation, no raw queries | ✅     |
| **A04: Insecure Design**        | Defense in depth, least privilege, input validation          | ✅     |
| **A05: Security Misconfig**     | Secure headers, no debug in prod, env validation             | ✅     |
| **A06: Vulnerable Components**  | Regular dependency updates, npm audit, Dependabot            | 🔄     |
| **A07: Auth Failures**          | NextAuth.js, OAuth 2.0, secure session management            | ✅     |
| **A08: Data Integrity**         | Server-side validation, revalidation on mutations            | ✅     |
| **A09: Logging Failures**       | Console logging (enhance with structured logging)            | 🔄     |
| **A10: SSRF**                   | No user-controlled URLs for server requests                  | ✅     |

### Authentication Flow Security

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        SECURE AUTHENTICATION FLOW                            │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  1. USER INITIATES LOGIN                                                     │
│  ┌──────────────┐                                                            │
│  │  Click       │──► Redirect to Google OAuth                                │
│  │  "Sign In"   │    (PKCE flow for SPA security)                           │
│  └──────────────┘                                                            │
│         │                                                                    │
│         ▼                                                                    │
│  2. GOOGLE AUTHENTICATION                                                    │
│  ┌──────────────────────────────────────────────────────────────────────┐   │
│  │  • User authenticates with Google                                    │   │
│  │  • Google validates credentials                                      │   │
│  │  • Returns authorization code (NOT tokens in URL)                    │   │
│  └──────────────────────────────────────────────────────────────────────┘   │
│         │                                                                    │
│         ▼                                                                    │
│  3. TOKEN EXCHANGE (Server-Side)                                             │
│  ┌──────────────────────────────────────────────────────────────────────┐   │
│  │  • NextAuth exchanges code for tokens (server-to-server)             │   │
│  │  • Tokens never exposed to client                                    │   │
│  │  • User info extracted from ID token                                 │   │
│  └──────────────────────────────────────────────────────────────────────┘   │
│         │                                                                    │
│         ▼                                                                    │
│  4. SESSION CREATION                                                         │
│  ┌──────────────────────────────────────────────────────────────────────┐   │
│  │  • User created/found in MongoDB                                     │   │
│  │  • JWT session token generated (signed with NEXTAUTH_SECRET)         │   │
│  │  • Token stored in HttpOnly, Secure, SameSite=Lax cookie             │   │
│  └──────────────────────────────────────────────────────────────────────┘   │
│         │                                                                    │
│         ▼                                                                    │
│  5. SESSION ENRICHMENT                                                       │
│  ┌──────────────────────────────────────────────────────────────────────┐   │
│  │  session callback:                                                   │   │
│  │  • Adds user.id (MongoDB ObjectId)                                   │   │
│  │  • Adds user.role (admin/user)                                       │   │
│  │  • No sensitive data in session                                      │   │
│  └──────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### Access Control Matrix

| Action            | Guest | User (Own) | User (Others) | User (Collab) | Admin |
| ----------------- | ----- | ---------- | ------------- | ------------- | ----- |
| View Public File  | ✅    | ✅         | ✅            | ✅            | ✅    |
| View Private File | ❌    | ✅         | ❌            | ❌            | ✅    |
| Create File       | ❌    | ✅ (≤5)    | -             | -             | ✅    |
| Edit File         | ❌    | ✅         | ❌            | ✅            | ✅    |
| Delete File       | ❌    | ✅         | ❌            | ❌            | ✅    |
| Change Settings   | ❌    | ✅         | ❌            | ❌            | ✅    |

### Security Headers Configuration

```typescript
// Recommended next.config.ts security headers
const securityHeaders = [
	{
		key: 'X-DNS-Prefetch-Control',
		value: 'on',
	},
	{
		key: 'Strict-Transport-Security',
		value: 'max-age=63072000; includeSubDomains; preload',
	},
	{
		key: 'X-Frame-Options',
		value: 'SAMEORIGIN',
	},
	{
		key: 'X-Content-Type-Options',
		value: 'nosniff',
	},
	{
		key: 'Referrer-Policy',
		value: 'origin-when-cross-origin',
	},
	{
		key: 'Permissions-Policy',
		value: 'camera=(), microphone=(), geolocation=()',
	},
	{
		key: 'Content-Security-Policy',
		value: "default-src 'self'; script-src 'self' 'unsafe-eval'; style-src 'self' 'unsafe-inline';",
	},
];
```

### Threat Model

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              THREAT MODEL                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ASSET: Code Files (User-generated content)                                  │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │  Threat                    │ Likelihood │ Impact │ Mitigation          │ │
│  ├────────────────────────────┼────────────┼────────┼─────────────────────┤ │
│  │  Unauthorized file access  │ Medium     │ High   │ Auth + RBAC         │ │
│  │  Data exfiltration         │ Low        │ High   │ Visibility controls │ │
│  │  Content tampering         │ Medium     │ Medium │ Permission checks   │ │
│  │  Malicious code injection  │ Low        │ High   │ Sanitization        │ │
│  └────────────────────────────┴────────────┴────────┴─────────────────────┘ │
│                                                                              │
│  ASSET: User Accounts                                                        │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │  Threat                    │ Likelihood │ Impact │ Mitigation          │ │
│  ├────────────────────────────┼────────────┼────────┼─────────────────────┤ │
│  │  Account takeover          │ Low        │ High   │ OAuth (no passwords)│ │
│  │  Session hijacking         │ Low        │ High   │ Secure cookies, JWT │ │
│  │  Privilege escalation      │ Low        │ Critical│ Server-side roles  │ │
│  │  Impersonation             │ Very Low   │ High   │ OAuth verification  │ │
│  └────────────────────────────┴────────────┴────────┴─────────────────────┘ │
│                                                                              │
│  ASSET: Application Infrastructure                                           │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │  Threat                    │ Likelihood │ Impact │ Mitigation          │ │
│  ├────────────────────────────┼────────────┼────────┼─────────────────────┤ │
│  │  DDoS attack               │ Medium     │ High   │ Edge protection     │ │
│  │  API abuse                 │ Medium     │ Medium │ Rate limiting       │ │
│  │  Database injection        │ Low        │ Critical│ Mongoose ODM       │ │
│  │  Secret exposure           │ Low        │ Critical│ Env vars, .gitignore│ │
│  └────────────────────────────┴────────────┴────────┴─────────────────────┘ │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### Security Checklist

- [x] HTTPS enforced (hosting provider)
- [x] Secure authentication (NextAuth + OAuth)
- [x] Authorization on all mutations
- [x] Input validation (Zod schemas)
- [x] Content sanitization
- [x] Secure session cookies
- [x] No secrets in client code
- [x] MongoDB injection prevention
- [x] XSS protection (React escaping)
- [x] CSRF protection (Server Actions)
- [ ] Rate limiting (recommended)
- [ ] Security headers (recommended)
- [ ] Audit logging (recommended)
- [ ] Dependency scanning (recommended)

---

## State Management

### State Distribution

```
┌─────────────────────────────────────────────────────────────────────┐
│                      STATE ARCHITECTURE                              │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │  SERVER STATE (SSR - No Hydration)                            │  │
│  │  ├── User Session (getServerSession)                          │  │
│  │  ├── File List (Dashboard - CodeFile.find)                    │  │
│  │  ├── File Data (Code Page - CodeFile.findById)                │  │
│  │  └── Permissions (canEdit computed on server)                 │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                                                                      │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │  CLIENT STATE (React useState/useRef)                         │  │
│  │  ├── Editor Content (useAutoSave - content, title)            │  │
│  │  ├── Save Status (isSaving, saveStatus, saveError)            │  │
│  │  ├── File Settings (language, visibility, editMode)           │  │
│  │  └── UI State (isDeleting, dropdown open states)              │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                                                                      │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │  CONTEXT STATE (React Context)                                │  │
│  │  └── SessionProvider (NextAuth session across components)     │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

### useAutoSave Hook Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                       useAutoSave Hook                               │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  INPUTS:                                                             │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │  fileId, initialContent, initialTitle, canEdit              │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                      │
│  INTERNAL STATE:                                                     │
│  ┌────────────────────┐  ┌────────────────────┐                     │
│  │  useState          │  │  useRef            │                     │
│  │  • content         │  │  • saveTimeoutRef  │                     │
│  │  • title           │  │  • lastSavedRef    │                     │
│  │  • isSaving        │  │  • pendingRef      │                     │
│  │  • saveStatus      │  │  • isMountedRef    │                     │
│  │  • saveError       │  │                    │                     │
│  └────────────────────┘  └────────────────────┘                     │
│                                                                      │
│  BEHAVIORS:                                                          │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │  save()          - Executes server action                   │    │
│  │  scheduleSave()  - Debounce wrapper (1500ms)                │    │
│  │  setContent()    - Update + trigger save                    │    │
│  │  setTitle()      - Update + trigger save                    │    │
│  │  resetState()    - Full state reset                         │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                      │
│  EFFECTS:                                                            │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │  Cleanup         - Clear timeout, sendBeacon on unmount     │    │
│  │  beforeunload    - Warn user of unsaved changes             │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                      │
│  OUTPUTS:                                                            │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │  { content, title, isSaving, saveStatus, saveError,         │    │
│  │    setContent, setTitle, resetState }                       │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

---

## Database Design

### Entity Relationship Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                        MongoDB Collections                           │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌─────────────────────────────┐                                    │
│  │          users              │                                    │
│  ├─────────────────────────────┤                                    │
│  │  _id: ObjectId (PK)         │                                    │
│  │  name: String (required)    │                                    │
│  │  email: String (unique)     │◄─────────────────────┐             │
│  │  image: String              │                      │             │
│  │  providerId: String (unique)│                      │             │
│  │  role: "admin" | "user"     │                      │ ref         │
│  │  createdAt: Date            │                      │             │
│  │  updatedAt: Date            │                      │             │
│  └─────────────────────────────┘                      │             │
│                                                        │             │
│  ┌─────────────────────────────┐                      │             │
│  │        codefiles            │                      │             │
│  ├─────────────────────────────┤                      │             │
│  │  _id: ObjectId (PK)         │                      │             │
│  │  title: String (required)   │                      │             │
│  │  language: String (default) │                      │             │
│  │  content: String            │                      │             │
│  │  visibility: "public"|      │                      │             │
│  │              "private"      │                      │             │
│  │  editMode: "owner"|         │                      │             │
│  │            "collaborative"  │                      │             │
│  │  createdBy: ObjectId (FK) ──┼──────────────────────┘             │
│  │  createdAt: Date            │                                    │
│  │  updatedAt: Date            │                                    │
│  └─────────────────────────────┘                                    │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

### Indexes (Implicit via Mongoose)

| Collection | Field      | Type   | Purpose             |
| ---------- | ---------- | ------ | ------------------- |
| users      | \_id       | PK     | Primary key         |
| users      | email      | Unique | Login lookup        |
| users      | providerId | Unique | OAuth deduplication |
| codefiles  | \_id       | PK     | Primary key         |
| codefiles  | createdBy  | Index  | User's file lookup  |
| codefiles  | createdAt  | Index  | Sorting (implicit)  |

---

## API Design

### Server Actions (Primary API)

| Action                   | Input                          | Output        | Auth Required |
| ------------------------ | ------------------------------ | ------------- | ------------- |
| `createCodeFile`         | `CodeFileInput`                | `string` (id) | ✅            |
| `updateCodeFile`         | `id`, `{content, title?}`      | `{success}`   | ✅ + canEdit  |
| `updateCodeFileSettings` | `id`, `Partial<CodeFileInput>` | `{success}`   | ✅ + canEdit  |
| `deleteCodeFile`         | `id`                           | `{success}`   | ✅ + isOwner  |

### REST API Routes

| Method | Endpoint                  | Purpose              | Auth Required |
| ------ | ------------------------- | -------------------- | ------------- |
| POST   | `/api/save-on-unload`     | Beacon save fallback | ✅ + canEdit  |
| GET    | `/api/auth/[...nextauth]` | NextAuth handlers    | -             |
| POST   | `/api/auth/[...nextauth]` | NextAuth handlers    | -             |

### Validation Schemas

```typescript
// Object ID Validation
objectIdSchema: z.string().regex(/^[a-f\d]{24}$/i);

// Code File Schema
codeFileSchema: z.object({
	title: z.string().min(1).max(100).transform(trim),
	language: z.string().min(1),
	content: z.string().max(500000).transform(sanitize),
	visibility: z.nativeEnum(FileVisibility),
	editMode: z.nativeEnum(FileEditMode),
});
```

---

## Design Patterns Used

| Pattern                 | Implementation               | Purpose                       |
| ----------------------- | ---------------------------- | ----------------------------- |
| **Singleton**           | `lib/db.ts`                  | Single MongoDB connection     |
| **Barrel Exports**      | `index.ts` files             | Clean imports                 |
| **Container/Presenter** | `FileEditor` / UI components | State/presentation separation |
| **Custom Hooks**        | `useAutoSave`, etc.          | Reusable stateful logic       |
| **Server Actions**      | `app/actions/`               | Type-safe RPC mutations       |
| **Enum Pattern**        | `enums/`                     | Type-safe constants           |
| **Strategy Pattern**    | Permission functions         | Flexible authorization        |

---

## Scalability Architecture

### Current Architecture (Single Instance)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         CURRENT DEPLOYMENT MODEL                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│    Users                                                                     │
│      │                                                                       │
│      ▼                                                                       │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │                         Vercel Edge Network                            │ │
│  │  • Global CDN for static assets                                        │ │
│  │  • Automatic SSL/TLS                                                   │ │
│  │  • DDoS protection                                                     │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                    │                                         │
│                                    ▼                                         │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │                    Next.js Serverless Functions                        │ │
│  │  • Auto-scaling based on traffic                                       │ │
│  │  • Cold start optimization with connection caching                     │ │
│  │  • Regional deployment (single region)                                 │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                    │                                         │
│                                    ▼                                         │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │                        MongoDB Atlas (M0-M10)                          │ │
│  │  • Single cluster                                                      │ │
│  │  • Auto-scaling storage                                                │ │
│  │  • Built-in replication                                                │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                                                              │
│  CAPACITY: ~1,000-10,000 concurrent users                                    │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### Scaled Architecture (High Traffic)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                      SCALED DEPLOYMENT MODEL (Future)                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│                              Global Users                                    │
│                                   │                                          │
│                                   ▼                                          │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │                    CDN / Edge Network (Cloudflare/Vercel)              │ │
│  │  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐      │ │
│  │  │ US-West │  │ US-East │  │  EU     │  │  Asia   │  │ Australia│     │ │
│  │  │  Edge   │  │  Edge   │  │  Edge   │  │  Edge   │  │  Edge   │      │ │
│  │  └────┬────┘  └────┬────┘  └────┬────┘  └────┬────┘  └────┬────┘      │ │
│  │       └───────────┬┴───────────┴┬───────────┴───────────┬┘            │ │
│  └───────────────────┼─────────────┼───────────────────────┼─────────────┘ │
│                      │             │                       │               │
│                      ▼             ▼                       ▼               │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                        Load Balancer                                │   │
│  │  • Geo-routing                                                      │   │
│  │  • Health checks                                                    │   │
│  │  • Rate limiting                                                    │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                      │             │                       │               │
│                      ▼             ▼                       ▼               │
│  ┌──────────────────────┐ ┌──────────────────────┐ ┌──────────────────────┐│
│  │  App Instance 1      │ │  App Instance 2      │ │  App Instance N      ││
│  │  (Region: US)        │ │  (Region: EU)        │ │  (Region: Asia)      ││
│  │  ┌────────────────┐  │ │  ┌────────────────┐  │ │  ┌────────────────┐  ││
│  │  │ Next.js Server │  │ │  │ Next.js Server │  │ │  │ Next.js Server │  ││
│  │  └────────────────┘  │ │  └────────────────┘  │ │  └────────────────┘  ││
│  └──────────┬───────────┘ └──────────┬───────────┘ └──────────┬───────────┘│
│             │                        │                        │            │
│             └────────────────────────┼────────────────────────┘            │
│                                      ▼                                     │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                         Redis Cluster                               │   │
│  │  • Session caching                                                  │   │
│  │  • Rate limiting state                                              │   │
│  │  • Real-time pub/sub (for collaboration)                           │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                      │                                     │
│                                      ▼                                     │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                    MongoDB Atlas (M30+ / Sharded)                   │   │
│  │  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐          │   │
│  │  │   Primary    │◄──►│  Secondary   │◄──►│  Secondary   │          │   │
│  │  │   (Write)    │    │   (Read)     │    │   (Read)     │          │   │
│  │  └──────────────┘    └──────────────┘    └──────────────┘          │   │
│  │                                                                     │   │
│  │  Optional: Sharding for >1TB data                                  │   │
│  │  ┌─────────┐  ┌─────────┐  ┌─────────┐                             │   │
│  │  │ Shard 1 │  │ Shard 2 │  │ Shard 3 │  (by userId hash)          │   │
│  │  └─────────┘  └─────────┘  └─────────┘                             │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
│  CAPACITY: 100,000+ concurrent users                                         │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### Horizontal Scaling Strategy

| Component         | Current                 | Scaled Solution                 | Trigger         |
| ----------------- | ----------------------- | ------------------------------- | --------------- |
| **Compute**       | Serverless (auto)       | Multi-region serverless         | >5K concurrent  |
| **Database**      | MongoDB M0-M10          | MongoDB M30+ with replicas      | >100K documents |
| **Caching**       | None                    | Redis Cluster                   | >1K req/s       |
| **Static Assets** | Vercel CDN              | Multi-CDN (Cloudflare + Vercel) | Global traffic  |
| **File Storage**  | MongoDB (content field) | S3/R2 + CDN for large files     | >10MB files     |
| **Real-time**     | Polling (auto-save)     | WebSocket + Redis Pub/Sub       | Collaboration   |

### Caching Strategy

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           CACHING LAYERS                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  Layer 1: Browser Cache                                                      │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │  • Static assets (JS, CSS, images): max-age=31536000, immutable        │ │
│  │  • HTML pages: no-cache, must-revalidate                               │ │
│  │  • Monaco Editor chunks: cached for session                            │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                                                              │
│  Layer 2: CDN Edge Cache (Vercel/Cloudflare)                                │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │  • Static pages: ISR with revalidation                                 │ │
│  │  • Public file previews: stale-while-revalidate                        │ │
│  │  • API responses: cache-control headers                                │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                                                              │
│  Layer 3: Application Cache (Redis - Future)                                │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │  • Session data: 24h TTL                                               │ │
│  │  • User file list: 5min TTL, invalidate on mutation                    │ │
│  │  • Permission checks: 1min TTL                                         │ │
│  │  • Rate limit counters: sliding window                                 │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                                                              │
│  Layer 4: Database Query Cache (MongoDB)                                    │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │  • Index-backed queries                                                │ │
│  │  • Connection pooling                                                  │ │
│  │  • Read preference: secondaryPreferred for reads                       │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### Database Scaling Path

```
Stage 1: Current (0-10K users)
┌─────────────────────────────────────────┐
│  MongoDB Atlas M0-M10 (Shared/Dedicated)│
│  • Single replica set                   │
│  • 10GB-50GB storage                    │
│  • Connection limit: 500                │
└─────────────────────────────────────────┘
                    │
                    ▼ (>10K users)
Stage 2: Growth (10K-100K users)
┌─────────────────────────────────────────┐
│  MongoDB Atlas M30-M50                  │
│  • 3-node replica set                   │
│  • 100GB-500GB storage                  │
│  • Read replicas for queries            │
│  • Add indexes:                         │
│    - codefiles.createdBy + updatedAt    │
│    - codefiles.visibility + createdAt   │
└─────────────────────────────────────────┘
                    │
                    ▼ (>100K users)
Stage 3: Scale (100K+ users)
┌─────────────────────────────────────────┐
│  MongoDB Atlas M60+ / Sharded           │
│  • Sharding key: createdBy (hashed)     │
│  • Multiple shards                      │
│  • Global clusters (multi-region)       │
│  • Archive old/inactive files to S3     │
└─────────────────────────────────────────┘
```

### Rate Limiting Strategy (Recommended)

```typescript
// lib/rate-limit.ts (Recommended Implementation)
interface RateLimitConfig {
	window: number; // Time window in seconds
	maxRequests: number; // Max requests per window
}

const RATE_LIMITS: Record<string, RateLimitConfig> = {
	// API endpoints
	'create-file': { window: 60, maxRequests: 10 }, // 10/min
	'update-file': { window: 1, maxRequests: 5 }, // 5/sec (auto-save)
	'delete-file': { window: 60, maxRequests: 20 }, // 20/min
	'save-on-unload': { window: 1, maxRequests: 2 }, // 2/sec

	// Auth endpoints
	'sign-in': { window: 300, maxRequests: 5 }, // 5/5min
	'sign-out': { window: 60, maxRequests: 10 }, // 10/min

	// General API
	'api-global': { window: 60, maxRequests: 100 }, // 100/min per user
};
```

### Performance Benchmarks

| Metric                       | Current Target | Scaled Target | Measurement      |
| ---------------------------- | -------------- | ------------- | ---------------- |
| **Time to First Byte**       | <200ms         | <100ms        | Lighthouse       |
| **Largest Contentful Paint** | <2.5s          | <1.5s         | Core Web Vitals  |
| **API Response (P95)**       | <500ms         | <200ms        | APM              |
| **Database Query (P95)**     | <100ms         | <50ms         | MongoDB Profiler |
| **Concurrent Users**         | 1,000          | 100,000+      | Load Testing     |
| **Uptime SLA**               | 99.9%          | 99.99%        | Monitoring       |

---

## Best Practices

### Code Organization Principles

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        CODE ORGANIZATION PRINCIPLES                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  1. SEPARATION OF CONCERNS                                                   │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │  components/  → UI rendering only (no business logic)                  │ │
│  │  hooks/       → Reusable stateful logic                                │ │
│  │  lib/         → Core utilities & configurations                        │ │
│  │  utils/       → Pure helper functions                                  │ │
│  │  actions/     → Server-side mutations                                  │ │
│  │  models/      → Data layer definitions                                 │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                                                              │
│  2. SINGLE RESPONSIBILITY                                                    │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │  ✓ Each file does ONE thing well                                       │ │
│  │  ✓ Components: render UI                                               │ │
│  │  ✓ Hooks: manage state                                                 │ │
│  │  ✓ Actions: handle mutations                                           │ │
│  │  ✓ Utils: transform data                                               │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                                                              │
│  3. DRY (Don't Repeat Yourself)                                              │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │  ✓ Barrel exports (index.ts) for clean imports                         │ │
│  │  ✓ Shared components in ui/ and shared/                                │ │
│  │  ✓ Constants centralized in lib/constants.ts                           │ │
│  │  ✓ Enums for type-safe constants                                       │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                                                              │
│  4. COLOCATION                                                               │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │  ✓ Related files live together (dashboard/, editor/)                   │ │
│  │  ✓ Types near their implementations                                    │ │
│  │  ✓ Loading states next to pages                                        │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### TypeScript Best Practices

```typescript
// ✅ GOOD: Strict typing with interfaces
interface FileEditorProps {
	file: CodeFile;
	canEdit: boolean;
	currentUserId?: string;
}

// ✅ GOOD: Enum for fixed values
enum FileVisibility {
	PUBLIC = 'public',
	PRIVATE = 'private',
}

// ✅ GOOD: Zod for runtime validation
const codeFileSchema = z.object({
	title: z.string().min(1).max(100),
	content: z.string().max(500000),
});

// ✅ GOOD: Type inference from schema
type CodeFileInput = z.infer<typeof codeFileSchema>;

// ❌ BAD: Using 'any'
// function processData(data: any) { ... }

// ✅ GOOD: Proper error typing
try {
	await updateCodeFile(id, data);
} catch (error) {
	if (error instanceof Error) {
		console.error(error.message);
	}
}
```

### React Best Practices

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          REACT BEST PRACTICES                                │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  1. SERVER VS CLIENT COMPONENTS                                              │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │  Default: Server Components (RSC)                                      │ │
│  │  ├── Navbar.tsx          - fetches session, renders navigation         │ │
│  │  ├── Dashboard/page.tsx  - fetches files, renders list                 │ │
│  │  └── Code/[id]/page.tsx  - fetches file, checks permissions            │ │
│  │                                                                         │ │
│  │  Client Only When Needed:                                              │ │
│  │  ├── FileEditor.tsx      - state management, event handlers            │ │
│  │  ├── CodeEditor.tsx      - Monaco Editor (browser API)                 │ │
│  │  └── Providers.tsx       - SessionProvider (React Context)             │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                                                              │
│  2. HOOKS RULES                                                              │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │  ✓ Call hooks at top level only                                        │ │
│  │  ✓ Call hooks from React functions only                                │ │
│  │  ✓ Use useCallback for event handlers passed to children               │ │
│  │  ✓ Use useMemo for expensive computations                              │ │
│  │  ✓ Use useRef for values that don't trigger re-renders                 │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                                                              │
│  3. STATE MANAGEMENT                                                         │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │  ✓ Lift state only when necessary                                      │ │
│  │  ✓ Keep state close to where it's used                                 │ │
│  │  ✓ Use refs for non-reactive values (timeouts, previous values)        │ │
│  │  ✓ Avoid prop drilling - use composition                               │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                                                              │
│  4. PERFORMANCE                                                              │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │  ✓ Lazy load heavy components (Monaco Editor)                          │ │
│  │  ✓ Use Suspense with loading fallbacks                                 │ │
│  │  ✓ Debounce expensive operations (auto-save)                           │ │
│  │  ✓ Memoize callbacks passed to children                                │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### Error Handling Strategy

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         ERROR HANDLING STRATEGY                              │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  Layer 1: Client-Side Errors                                                 │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │  • error.tsx      - Route-level error boundary                         │ │
│  │  • global-error.tsx - Root layout errors                               │ │
│  │  • try/catch in event handlers                                         │ │
│  │  • User-friendly error messages                                        │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                                                              │
│  Layer 2: Server Action Errors                                               │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │  Pattern:                                                              │ │
│  │  ┌──────────────────────────────────────────────────────────────────┐  │ │
│  │  │  try {                                                           │  │ │
│  │  │    // Validate input                                             │  │ │
│  │  │    const validated = schema.parse(data);                         │  │ │
│  │  │    // Check permissions                                          │  │ │
│  │  │    if (!canEdit) throw new Error('Forbidden');                   │  │ │
│  │  │    // Execute operation                                          │  │ │
│  │  │    return { success: true, data };                               │  │ │
│  │  │  } catch (error) {                                               │  │ │
│  │  │    if (error instanceof z.ZodError) {                            │  │ │
│  │  │      throw new Error('Validation failed');                       │  │ │
│  │  │    }                                                             │  │ │
│  │  │    throw error;                                                  │  │ │
│  │  │  }                                                               │  │ │
│  │  └──────────────────────────────────────────────────────────────────┘  │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                                                              │
│  Layer 3: API Route Errors                                                   │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │  • Return proper HTTP status codes                                     │ │
│  │  • Structured error responses: { error: string, code?: string }        │ │
│  │  • Log server-side errors                                              │ │
│  │  • Never expose stack traces in production                             │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                                                              │
│  Error Response Standards:                                                   │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │  400 Bad Request    - Invalid input (Zod validation failed)           │ │
│  │  401 Unauthorized   - Not authenticated                                │ │
│  │  403 Forbidden      - Authenticated but not authorized                 │ │
│  │  404 Not Found      - Resource doesn't exist                           │ │
│  │  500 Internal Error - Unexpected server error                          │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### Code Quality Checklist

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         CODE QUALITY CHECKLIST                               │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  Pre-Commit (Automated via Husky + lint-staged):                             │
│  ├── [x] ESLint passes                                                       │
│  ├── [x] Prettier formatting applied                                         │
│  ├── [x] TypeScript compiles without errors                                  │
│  └── [x] No console.log in production code                                   │
│                                                                              │
│  Code Review Checklist:                                                      │
│  ├── [ ] Types are properly defined (no 'any')                               │
│  ├── [ ] Error cases are handled                                             │
│  ├── [ ] Loading states are considered                                       │
│  ├── [ ] Permissions are checked server-side                                 │
│  ├── [ ] Input is validated before use                                       │
│  ├── [ ] No sensitive data logged/exposed                                    │
│  ├── [ ] Components follow single responsibility                             │
│  ├── [ ] Hooks follow rules of hooks                                         │
│  ├── [ ] Memoization used where appropriate                                  │
│  └── [ ] Tests cover critical paths                                          │
│                                                                              │
│  Deployment Checklist:                                                       │
│  ├── [ ] Environment variables configured                                    │
│  ├── [ ] Database migrations applied                                         │
│  ├── [ ] Build succeeds without warnings                                     │
│  ├── [ ] Lighthouse score >90                                                │
│  └── [ ] Smoke tests pass                                                    │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### Naming Conventions

| Type                   | Convention         | Example                            |
| ---------------------- | ------------------ | ---------------------------------- |
| **Components**         | PascalCase         | `FileEditor`, `CreateFileButton`   |
| **Hooks**              | camelCase + use    | `useAutoSave`, `useClipboard`      |
| **Files (components)** | PascalCase.tsx     | `FileEditor.tsx`                   |
| **Files (utils)**      | camelCase.ts       | `validations.ts`                   |
| **Enums**              | PascalCase         | `FileVisibility`                   |
| **Enum values**        | UPPER_SNAKE_CASE   | `FileVisibility.PUBLIC`            |
| **Interfaces**         | PascalCase + I     | `ICodeFile`, `IUser`               |
| **Types (props)**      | PascalCase + Props | `FileEditorProps`                  |
| **Constants**          | UPPER_SNAKE_CASE   | `MAX_CONTENT_LENGTH`               |
| **Server Actions**     | camelCase + verb   | `createCodeFile`, `updateCodeFile` |
| **CSS classes**        | kebab-case         | `file-card`, `save-status`         |

---

## Testing Strategy

### Testing Pyramid

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           TESTING PYRAMID                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│                              ┌───────┐                                       │
│                             /   E2E   \        5-10% of tests               │
│                            /  (Cypress) \      - Critical user flows         │
│                           /───────────────\    - Auth flow, CRUD             │
│                          /                 \                                 │
│                         /   Integration     \   20-30% of tests              │
│                        /   (Jest + RTL)      \  - Component interactions     │
│                       /───────────────────────\ - API route testing          │
│                      /                         \                             │
│                     /         Unit Tests        \   60-70% of tests          │
│                    /        (Jest + Vitest)      \  - Utils, hooks, schemas  │
│                   /───────────────────────────────\ - Pure functions         │
│                  /                                 \                         │
│                 └───────────────────────────────────┘                        │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### Test Coverage Targets

| Layer              | Target   | Tools                 | Focus Areas                   |
| ------------------ | -------- | --------------------- | ----------------------------- |
| **Utils**          | 90%+     | Jest/Vitest           | Validations, date formatting  |
| **Hooks**          | 80%+     | React Testing Library | useAutoSave, useClipboard     |
| **Components**     | 70%+     | RTL + Jest            | UI rendering, interactions    |
| **Server Actions** | 80%+     | Jest + MSW            | CRUD operations, permissions  |
| **API Routes**     | 80%+     | Supertest             | HTTP status codes, responses  |
| **E2E**            | Critical | Cypress/Playwright    | Auth, create/edit/delete flow |

### Example Test Structure

```typescript
// __tests__/utils/validations.test.ts
describe('codeFileSchema', () => {
  it('validates correct input', () => {
    const input = { title: 'Test', language: 'javascript', content: '' };
    expect(() => codeFileSchema.parse(input)).not.toThrow();
  });

  it('rejects empty title', () => {
    const input = { title: '', language: 'javascript' };
    expect(() => codeFileSchema.parse(input)).toThrow();
  });

  it('sanitizes content with null bytes', () => {
    const input = { title: 'Test', language: 'js', content: 'hello\x00world' };
    const result = codeFileSchema.parse(input);
    expect(result.content).toBe('helloworld');
  });
});

// __tests__/hooks/useAutoSave.test.ts
describe('useAutoSave', () => {
  it('debounces save calls', async () => {
    const { result } = renderHook(() => useAutoSave({ ... }));

    act(() => result.current.setContent('a'));
    act(() => result.current.setContent('ab'));
    act(() => result.current.setContent('abc'));

    // Only one save should be scheduled
    await waitFor(() => expect(mockUpdateCodeFile).toHaveBeenCalledTimes(1));
  });
});
```

---

## Observability & Monitoring

### Logging Strategy

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           LOGGING STRATEGY                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  Log Levels:                                                                 │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │  ERROR   - Exceptions, failed operations, security events             │ │
│  │  WARN    - Deprecated usage, retry attempts, slow queries             │ │
│  │  INFO    - User actions, API calls, state changes                     │ │
│  │  DEBUG   - Detailed flow, variable values (dev only)                  │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                                                              │
│  Structured Logging Format:                                                  │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │  {                                                                     │ │
│  │    "timestamp": "2025-12-28T10:30:00Z",                               │ │
│  │    "level": "INFO",                                                    │ │
│  │    "service": "code-share",                                            │ │
│  │    "action": "file.create",                                            │ │
│  │    "userId": "abc123",                                                 │ │
│  │    "fileId": "xyz789",                                                 │ │
│  │    "duration_ms": 45,                                                  │ │
│  │    "status": "success"                                                 │ │
│  │  }                                                                     │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                                                              │
│  What to Log:                                                                │
│  ├── Authentication events (login, logout, failures)                        │
│  ├── Authorization failures (403 responses)                                 │
│  ├── CRUD operations with user context                                      │
│  ├── API errors with stack traces (server-side only)                        │
│  ├── Performance metrics (slow queries, timeouts)                           │
│  └── Security events (rate limiting, suspicious activity)                   │
│                                                                              │
│  What NOT to Log:                                                            │
│  ├── Passwords, tokens, secrets                                              │
│  ├── Full file content                                                       │
│  ├── PII beyond necessary identifiers                                        │
│  └── Debug logs in production                                                │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### Monitoring Stack (Recommended)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        MONITORING ARCHITECTURE                               │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │                        Application                                     │ │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐                 │ │
│  │  │   Logging    │  │   Metrics    │  │   Tracing    │                 │ │
│  │  │  (Winston)   │  │  (Custom)    │  │  (OpenTel)   │                 │ │
│  │  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘                 │ │
│  └─────────┼─────────────────┼─────────────────┼─────────────────────────┘ │
│            │                 │                 │                           │
│            ▼                 ▼                 ▼                           │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                    Observability Platform                           │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐                 │   │
│  │  │   Sentry    │  │  Vercel     │  │  DataDog/   │                 │   │
│  │  │   (Errors)  │  │  Analytics  │  │  Grafana    │                 │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘                 │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                       │
│                                    ▼                                       │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                         Alerting                                    │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐                 │   │
│  │  │   Slack     │  │   Email     │  │  PagerDuty  │                 │   │
│  │  │   Alerts    │  │   Alerts    │  │   On-Call   │                 │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘                 │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### Key Metrics Dashboard

| Category         | Metric                    | Target   | Alert Threshold |
| ---------------- | ------------------------- | -------- | --------------- |
| **Availability** | Uptime                    | 99.9%    | <99.5%          |
| **Performance**  | P95 Response Time         | <500ms   | >1000ms         |
| **Performance**  | TTFB (Time to First Byte) | <200ms   | >500ms          |
| **Errors**       | Error Rate                | <0.1%    | >1%             |
| **Errors**       | 5xx Responses             | <0.01%   | >0.1%           |
| **Database**     | Query Time P95            | <100ms   | >500ms          |
| **Database**     | Connection Pool Usage     | <70%     | >90%            |
| **Business**     | Files Created/hour        | Baseline | -50% or +500%   |
| **Business**     | Active Users/day          | Baseline | -30%            |

### Health Checks

```typescript
// app/api/health/route.ts (Recommended)
export async function GET() {
  const checks = {
    status: 'healthy',
    timestamp: new Date().toISOString(),
    version: process.env.npm_package_version,
    checks: {
      database: await checkDatabase(),
      memory: checkMemory(),
      uptime: process.uptime(),
    }
  };

  const isHealthy = Object.values(checks.checks)
    .every(check => check.status === 'ok');

  return Response.json(checks, {
    status: isHealthy ? 200 : 503
  });
}

async function checkDatabase() {
  try {
    await connectDB();
    await mongoose.connection.db.admin().ping();
    return { status: 'ok', latency_ms: /* measured */ };
  } catch {
    return { status: 'error', error: 'Connection failed' };
  }
}
```

---

## Performance Considerations

1. **Server Components by Default** - Reduces client bundle size
2. **Connection Caching** - MongoDB singleton prevents cold starts
3. **Debounced Auto-Save** - 1500ms debounce reduces API calls
4. **Suspense Loading** - Graceful loading states
5. **Revalidation** - Targeted `revalidatePath` instead of full cache clear
6. **Monaco Lazy Loading** - Editor loads only when needed
7. **Image Optimization** - Next.js Image component with WebP
8. **Font Optimization** - next/font with font-display: swap

---

## Future Architecture Considerations

1. **Real-time Collaboration** - WebSocket/Socket.io for live editing
2. **Rate Limiting** - Upstash Redis for serverless rate limiting
3. **Caching Layer** - Redis for session/file caching
4. **CDN** - Multi-CDN strategy for global performance
5. **Monitoring** - Sentry for errors, Vercel Analytics for performance
6. **Testing** - Jest + RTL + Cypress for comprehensive coverage
7. **CI/CD** - GitHub Actions for automated testing and deployment
8. **Feature Flags** - LaunchDarkly/Vercel Edge Config for gradual rollouts

---

_Last Updated: December 2025_
